---
id: 007
kind: experiment
status: done
one_line: connecting theorem — tie the orphan NonzeroResponseRHScale to ProjectedRenewalQuadraticBoundedStatement, closing the definition-drift gap
class: D
parent: 003
---

## Declaration (frozen at launch commit)

target: a kernel-checked Lean theorem in mobius-synthesis (our local
  `agent-os` branch) connecting the orphan predicate to the
  bridge-consumed statement. Primary form (either direction counts;
  both = full success):
  `NonzeroResponseRHScale → ProjectedRenewalQuadraticBoundedStatement Λ`
  (for the Λ the bridge needs, or ∀ Λ ≥ 0), and/or the converse.
  Statement adjustments (ε-management, block-membership bookkeeping)
  are permitted so long as the theorem genuinely ties the two named
  predicates; a theorem about renamed lookalikes does not count
  (definition-drift check applies to us too).
approach: the repo already proves
  `localSequenceEnergy_sourceHighPrefix_eq_projectedRenewalGramValue`
  (window Gram value = Σ_{r<H} |scalar face|²), so the scalar→quadratic
  direction should be summing the scalar bound over the window with
  X_{N+r} ≤ (N+H+1)² ≤ (2N+1)² absorbed into C·N^{2+ε}; the converse
  specializes H = 1. Work in a NEW module importing both sides; do not
  touch existing files.
analogue: none needed — finite algebra + monotonicity; this is
  formalization-support, not an analytic technique.
obstacles_addressed: none of the declared analytic obstacles apply
  (no estimate on the open statement is attempted); the relevant
  hazard is definition drift, addressed by naming the two exact
  predicates in the theorem statement verbatim.
decision_rule: kernel-green proof of ≥1 direction ⇒ orphan gap closed
  (frontier delta: none — labelled formalization-support; also a
  report item for Fred). Failure after budget ⇒ closes nothing
  mathematical; records the mismatch precisely (which quantifier/cast
  blocks it), which is itself the report item. No closure of any
  attack angle either way.
promotion_gate: `lake build RHLean --wfail` green with the new module
  imported, clean `audit_assumptions.sh`, and `#print axioms` on the
  new theorem(s) showing only [propext, Classical.choice, Quot.sound]
  — at a named commit on local mobius-synthesis `agent-os`.
viability_gate: agent reports concrete progress (statement elaborated,
  sorry count decreasing) within the budget; one relaunch permitted on
  a specific blocker.
budget: one implementation agent run (+ one retry), EC2 build cycles.
review: waived per PROOF_OS ("never convene a panel to ask whether a
  file compiles") — the kernel is the entire verdict here; recorded
  as waiver.

## Gate revisions

(none)

## Run

(appended)

## Verdict

(appended after kernel evidence)

## Run

- Attack agent: 3 build iterations; new module
  `RHLean/Analysis/MobiusSynthesisBoundaryBridge.lean` (894 lines, no
  existing files modified) at mobius-synthesis `agent-os` commit
  `6026165`. Agent evidence: `build-log.txt`, `axiom-check.txt`.
- Coordinator gate execution (independent re-verification): manifest
  import added (`59c7fdd`, 250 imports — includes the formerly orphan
  `MobiusSynthesisBoundary`); full `lake build RHLean --wfail` on
  mobius-fleet-1: green, 7602 jobs; `audit_assumptions.sh`: passed;
  fresh `#print axioms` on all three public theorems:
  `[propext, Classical.choice, Quot.sound]`
  (`coordinator_axiom_recheck.txt`).

## Verdict

deployment_status: **promoted** — the connecting equivalence
`NonzeroResponseRHScale ↔ (∀ Λ ≥ 0, ProjectedRenewalQuadraticBoundedStatement Λ)`
is kernel-proved at named commits (6026165 + 59c7fdd, local branch
only). The definition-drift orphan gap (record 003) is closed: the
README's headline predicate and the bridge-consumed statement are now
formally the same open problem, and the orphan module is in the build
and audited.

Mathematical route (new content): the scalar face links to the Gram
chain through `primorialMinimalSquareWheelNonzeroResponse_eq_mertensCenter`
+ the ρ ≤ (X+1)/(3(U+1)) coupling bound + square-gap interpolation,
then the repo's existing iff chain to the quadratic statement; the
converse specializes back through the Mertens energy bound.

frontier_delta: **none** — labelled `formalization-support` per the
closure-theatre guard. The open analytic content is untouched; what
changed is that the program (and Fred's README) can now cite one
target instead of two disconnected ones. candidate_status: retained.
closure: n/a (success). untested_axes: n/a. This is the program's
first original kernel-checked contribution; report item for Fred.

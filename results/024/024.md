---
id: 024
kind: experiment
status: done
one_line: kernel formalization of the backward affine excursion package (023 frozen plan) — square-pin stability, affine modulus, excursion/moment transfer
parent: 023
class: H
---

Formalize, in mobius-synthesis (branch agent-os, new module
RHLean/Analysis/PrimeSieveBackwardAffineExcursion.lean), the
bounded-scope items frozen in record 023:

1. **Exact square-pin backward stability:** for x₀ = (y+1)²−1 and
   0 ≤ t ≤ y: ⌊(x₀−t)/(y+1)⌋ = y (pure Nat arithmetic).
2. **h=1 divisor-jump identity:** ⌊(x+1)/d⌋ − ⌊x/d⌋ = 1 iff d ∣ x+1,
   else 0; general bound ⌊(x+h)/d⌋ − ⌊x/d⌋ ≤ h/d + 1 in the exact
   Nat form.
3. **Backward affine modulus:** on a stable backward window, the
   Möbius discrepancy sum satisfies
   |S_y(x₀−t) − S_y(x₀)| ≤ A_y·t + B_y with the record-023 constants
   (harmonic-sum slope, K/log(y+1) intercept), stated against the
   repo's existing primeSieveMoebiusDiscrepancySum objects.
4. **Abstract affine excursion lemma:** H = |f(x₀)|, B < H/2,
   |f(x₀±t) − f(x₀)| ≤ At + B ⟹ |f(x₀±t)| ≥ H/2 for t ≤ (H/2−B)/A
   (reverse triangle inequality) + the moment corollary
   (H/2)^{2k}·W ≤ Σ_{t<W} |f|^{2k}.
5. **Backward canonical-window theorem:** items 1+3+4 assembled at
   the canonical pin — the record-020 machinery's backward version,
   no discrepancy envelope needed.

Gate (unchanged program gate): lake build RHLean --wfail green with
the module imported in RHLean.lean + audit_assumptions.sh pass +
#print axioms = [propext, Classical.choice, Quot.sound] at a named
commit. Numerics: none. EC2: build channel only. Promotion is
kernel-only; anything not proved lands as clearly-marked hypotheses
or is dropped from the record as unproved.

## Run

Module RHLean/Analysis/PrimeSieveBackwardAffineExcursion.lean authored
(coordinator), built on EC2 warm tree; 3 build iterations (namespace
convention, omega-vs-variable-division, noncomputable section).

## Results

**PROMOTED — kernel theorem set #5, commit b26e73e (mobius-synthesis
agent-os).** Gate: lake build RHLean --wfail green (7612 jobs),
audit_assumptions.sh pass, #print axioms = [propext, Classical.choice,
Quot.sound] on all six named theorems. Contents:

- `primeSieveCanonicalPin_backward_stable`: ⌊(x₀−t)/(y+1)⌋ = y for
  all t ≤ y at x₀ = (y+1)²−1 — the exact backward stability that the
  forward machinery lacked.
- `floor_succ_div_sub_eq_divisor_indicator`: the h=1 floor jump is
  exactly the divisor indicator of x+1 (023's D2 core, kernel form).
- `floor_add_div_sub_le`: sharp per-d bound ⌊(x+h)/d⌋−⌊x/d⌋ ≤ h/d+1.
- `primeSieveLipschitzConstant_mono` + backward increment bound
  ‖S(y,x₀) − S(y,x₀−t)‖ ≤ C(y,x₀)·(t+1), t ≤ y, no stability
  hypothesis (the pin supplies it).
- `primeSieveBackwardWindow` = min(y+1, ⌊H/2C⌋) with excursion
  theorem (H/2 persists on the whole backward window) and the
  2k-th moment lower bound (H/2)^{2k}·W ≤ Σ_{t<W}‖S(y,x₀−t)‖^{2k} —
  record 020's transfer with the vacuous forward hypotheses REMOVED
  at canonical pins: hypotheses are now just 1 ≤ y.
- `primeSieveMoebiusDiscrepancySum_support_insertion`: exact identity
  S(y,x+1) − S(y,x) = frozen-support increment + μ(K+1)·R(y+1) at
  the transition x+1 = (K+1)(y+1) (023's D3 decomposition, kernel
  form).

All five declared items delivered; nothing dropped, no extra
hypotheses smuggled in. At canonical pins the excursion window is now
W = min(y+1, ⌊H/2C⌋) ≍ min(√x, H·log x/√x): with H ≍ y (the
record-023 scale) this is W ≍ √x — the upgrade from W ≍ log x.
Frontier delta: **formalization-support + machinery-upgrade** (the
pinned-to-windowed transfer now actually fires at the program's own
sample points). Open half unchanged: the windowed moment UPPER bound
(what the excursion lower bound must be played against) remains the
hard analytic object.

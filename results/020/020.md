---
id: 020
kind: experiment
status: done
one_line: H1 rung-0 — kernel-formalize the Lipschitz excursion lemma and the pinned→windowed moment-hierarchy reduction
class: H1
parent: 018
---

## Declaration (frozen at launch commit)
target: Lean theorems in mobius-synthesis (agent-os): (1) the
  unconditional O(1)-Lipschitz property of the Abel-face object
  (increments of Σ_{d≤K}μ(d)R(⌊x/d⌋)-type sums under x → x+h
  controlled linearly in h with explicit constant, per agent0.json's
  counting argument: π-increments over d ≤ K count integers in
  (x, x+h] with a >√x prime factor ≤ 2h pairs + Li drift O(h)); (2)
  the excursion lemma: a pinned value of height H forces the object
  to exceed H/2 on an interval of length ≥ H/(2C), hence
  H^{2k+1} ≤ C'·∫window |·|^{2k} — the pinned→averaged transfer that
  deletes obstacle 3 (statement adjusted to the repo's discrete
  setting; report deviations).
approach: elementary Finset/real-analysis arguments over the
  kernel-proved Abel identity module (015); no analytic machinery.
analogue: standard excursion/Chebyshev arguments (cited).
obstacles_addressed: pinned-vs-averaged is the SUBJECT (the lemma
  removes it); nothing else binds (exact/elementary).
decision_rule: kernel-green ⇒ pinned target formally reduced to
  windowed moment control (frontier delta: conditional-bridge —
  honest label; the moments are open). Failure ⇒ exact blocking goal
  recorded.
promotion_gate: law 5 full gate at named commit.
viability_gate: sorry count decreasing; one relaunch on named blocker.
budget: one agent run + one retry.
review: waived (kernel-answerable).
## Gate revisions
(none)
## Run

Opus agent, 3 iterations (no mathematical rework). New module
`PrimeSieveLipschitzExcursion.lean` (594 lines) at mobius-synthesis
agent-os `b0b44b0`. Gate green (7605 jobs, audit pass, 13 public
results baseline-axioms); coordinator recheck clean
(`coordinator_axiom_recheck.txt`).

## Verdict

deployment_status: **promoted** (fourth kernel contribution) — with
substantive honest deviations from the informal source, which are
themselves the finding:
- D1 (better than claimed): the prime-side pair count is ≤1 per
  integer under x+h < (y+1)² (unique large-prime representation) —
  total prime-side jump ≤ h exactly.
- D2 (informal claim FAILS in discrete conventions): the Li drift is
  NOT O(h) — the kernel object evaluates Li at ⌊x/d⌋ and the floors
  jump on divisors; honest constant C(y,x) = 1 + K/log(y+1) ≈
  2√x/log x, not O(1). agent0's O(1)-Lipschitz claim is false as
  stated for the repo's object.
- D3: support-growth terms need a stability hypothesis
  (x % (y+1) + h < y+1), checkable; killing them needs PNT-strength
  input (not elementary).
- Excursion/moment transfer: proved for all k, sharpened (no H ≥ 2C
  hypothesis needed for the bounds).

Consequence: **obstacle 3 (pinned-vs-averaged) is formally deleted AS
A REDUCTION** — pinned control now provably follows from windowed
moment control. But the transfer's strength at the proved constant is
weak (pin √x ⇒ window ≈ log x/4); the H1 power-saving route needs C
improved from √x/log x toward O(polylog), which requires closing
D2+D3 — a real, well-defined open subproblem, not rung-0 elementary.
frontier_delta: conditional-bridge (honest label). candidate_status:
retained. Fourth time the kernel channel has corrected informal
mathematics this session (010's multiplicity, 006→009's drift
attribution, 013's critical range, now agent0's O(1) constant).

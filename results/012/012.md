---
id: 012
kind: diagnostic
status: done
one_line: G2 corrected first-contact — formulate the pair-to-root collision map precisely and prove/check a corrected fiber bound (review 011 alternative 1)
class: G2
parent: 011
---

Formulation pass, per review 011: the nonstandard hinge of angle G2
is not the textbook exponential sum but the MAP from off-diagonal
pairs (d,d′,a,a′) to a shared modulus with bounded fibers. Deliverable
is paper-level (informal channel — UNVERIFIED until reviewed/kernel):

1. Define every variable in the expanded off-diagonal sum; the
   reduction (a/d − a′/d′) = b/q; the alleged root datum; and the map
   pairs → datum, exactly.
2. State and prove (paper) a corrected fiber bound with honest
   hypotheses — reviewer's menu: squarefree q, or coprime-w with
   2^{ω(q)+1}, or the uniform 2^{ω(q)+1}·√gcd(w,q).
3. Symbolic checks at q = 8 and q = 9 (the review's counterexample
   moduli) and exhaustive fiber enumeration for one small dyadic D —
   any fiber growth beyond the corrected bound kills the transfer.
4. Establish or refute: are the arising reduced denominators q
   genuinely small (packet-conductor regime) or can they be large?
   (The modulus-magnitude obstacle forbids assuming smallness.)

Producer: Codex reasoning task (sandbox-safe; pure math + small
enumerations). Output lands here; verdict on the G2 angle's viability
follows from whether a correct bounded-fiber statement exists at all.
Numerics declaration: exact integer enumeration only; ranges declared
in output.

## Run
(appended)
## Results
(appended)

## Run

Codex task mslykodm-1ww0jq (9m11s; completion signal lost again — ops
pattern: THREE losses now; the status/result CLI is the reliable
channel). Full report: `codex_report.md` (session 019fe724-d1b2).

## Results

**VERDICT: MAP-FAILS — coordinator-verified by exact arithmetic.**
1. The map is well-defined and the conductor bound q ≤ lcm(d,d′) ≤ 4D²
   is proved and sharp — but D² is the WRONG SCALE for the parent's
   Heath–Brown step (which pays M+N at modulus scale M), before even
   reaching parity/squarefree-ness requirements.
2. Corrected root bounds (Theorem A) bound sample-residue repetitions,
   NOT divisor-pair fibers: at D=8, exact enumeration gives max tuple
   fiber 43 vs corrected root bound ≈ 8√6; q=8 fiber 17 vs bound 4.
3. Explicit witness family (q=t(t+1); m=qℓ−1; d=(t+1)m, d′=tm;
   a=(t+1)ℓ+1, a′=1): every tuple reduces to exactly 1/q — coordinator
   re-verified symbolically for t ∈ {2,3,5,8}, ℓ ≤ 60 — and the fiber
   at (6,1) grows as ~D/9 (verified to D=10⁵). Exact counterexample ⇒
   the universally-quantified bounded-fiber statement is CLOSED
   OUTRIGHT (numerics channel, exact-integer model).

**Consequence for angle G2:** the dispersion transfer as proposed
(quadratic-root multiplicity ⇒ sub-square pair map ⇒ Heath–Brown) is
dead — quadratic roots count the wrong object and conductors live at
D². G2 survives only if the H=1 exponent-flow audit (review 011
alternative 2, next record) finds a repaired architecture that
contracts the Mertens exponent without the bounded-fiber mechanism.
candidate_status: retained (map definition, conductor bound, and
enumeration harness reusable).

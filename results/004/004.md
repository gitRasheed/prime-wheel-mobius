---
id: 004
kind: diagnostic
status: done
one_line: Phase 1 recon — exact H_{k,n}, C, E over feasible blocks; test the route doc's C/E-correlation claim; map cancellation in the reciprocal-d family
class: D
parent: 003
---

Phase 1 reconnaissance on the canonical synthesis target. The route
doc (`mobius-synthesis/CURRENT_PROOF_ROUTE.md` §5–6) asserts, from
unpublished finite diagnostics, that C and E are positively correlated
with the same sign and that this correlation is where the C − 2E
cancellation lives. Nobody's data for this is in any repo. This
diagnostic reproduces it independently and maps the landscape the
attack angles will need.

## Method (declared before running)

1. **Definition extraction (agent, read-only):** from the synthesis
   Lean source, the exact computational content of
   `primorialMinimalWheelSystem k` (modulus Q_k), `primorialBlockLower/
   Upper`, `squarePrefixEndpoint`, the corrected residual R_k, the
   zero-mode centering operator `primorialSquareZeroModeCenter`,
   `primeSievePNTCorrectedAllPlusMass`, `primeSievePNTError`, and the
   PNT cutoff `primorialPNTPrimeSieveCutoff` — turned into a numeric
   spec with no interpretation drift.
2. **Computation (EC2, under telemetry):** Möbius sieve to W_9 =
   223,092,870 (numpy, exact integer μ). For every synchronized block
   k ≥ 2 and every complete-square sample X_n in range: exact
   H_{k,n} via the kernel-proved elimination identity; exact/high-
   precision C_{k,n}, E_{k,n} per the extracted definitions (Li terms
   are real-valued — mpmath/float128 with declared error bounds);
   cross-check H = C − 2E at every sample (the implementation gate).
3. **Analyses:** |H| vs √X_n scaling profile; C/E correlation
   (Pearson + sign-agreement rate) per block and pooled; reciprocal-d
   family decomposition of E (which d-ranges carry mass/cancellation);
   all artifacts CSV + SVG.

## Numerics-channel declaration

Exact integers for μ, M, H, all combinatorial masses; IEEE float64/
extended precision for Li with reported max absolute error; range
x ≤ W_9 (blocks k = 2..9); deterministic. Reconnaissance only —
promotes nothing, closes nothing; finite-range-deception caveat
applies to every scaling read. Utilization telemetry recorded per the
2026-08-09 logging directive; a <60% avg-CPU phase triggers a
parallelize-or-downscale review note.

Producer: `results/004/` scripts (committed with outputs);
compute host: mobius-fleet-1.

## Run

- 2026-08-09: definition-extraction agent complete — full computational
  spec with worked micro-example (k=2, n=2: H = −224/225; E(6,8) =
  1 − (Li(8)−Li(6))), verbatim in the journal-adjacent agent report;
  key conventions: Li anchored at 2 (not principal-value li), PNT bulk
  sums densities over ALL integers q, y_k = ⌊√U_k⌋+1, Q_k = smallest
  multiple of P_k = ∏_{p≤⌊√U_k⌋}p² exceeding U_k, and H needs only
  three Mertens values + one exact rational.
- 2026-08-09: μ sieve to W_9 = 223,092,870 on mobius-fleet-1: 12.2 s,
  M(W_9) = 3516 (`sieve_mu.py`, `sieve.log`).
- 2026-08-09: `recon_hce.py` launched (14-way multiprocessing,
  micro-example values embedded as hard asserts; phase tag
  `phase1-recon` in telemetry). Computes all square samples for
  k = 2..8.

## Results (2026-08-09; artifacts in this directory + `summary.json`)

Every complete-square sample in every synchronized block k = 2..8 —
~12,000 samples, X up to 223,092,870. Implementation gates all green:
the worked-micro-example asserts (H(2,2) = −224/225 exact; E(6,8) =
1 − (Li(8)−Li(6))), the Mertens checkpoints, and the kernel-proved
identity H = C_centered − 2·E_centered verified at **every** sample
(max abs err 2.8e-8 at k=8, pure Li-quadrature float accumulation;
≤ 6e-10 for k ≤ 7).

**The route doc's central claim is CONFIRMED in range.** corr(C, E)
per block: 0.87 (k=3), 0.83 (k=4), … 0.89 (k=7), **0.94 (k=8,
11,822 samples)** — rising with scale. Sign agreement 0.65–0.88. Mean
cancellation ratio |C−2E|/(|C|+2|E|) ≈ 0.47–0.59: the signed
combination runs at roughly half the mass of its parts, so the
triangle-inequality route demonstrably discards the phenomenon. The
correlation the synthesis route bets on is real in independently
implemented, kernel-extracted-definition data.

**Target scale.** sup |H|/√X per block: 0.35, 0.40, 0.35, …, 0.42
(k=7), 0.51 (k=8); means ~0.12–0.18. Sub-√ everywhere in range with a
mild upward drift in the sup — worth tracking, not evidence
(finite-range-deception caveat in force, as declared).

**Reciprocal-d landscape** (`dprofile_k*.csv`): per-sample top-40
contributions by |M(d)·discrepancy| captured for 12 samples per block —
the raw material for the dispersion-style angles the panel may propose.

**Utilization (per the 2026-08-09 logging directive): the box is
massively oversized for Phase-1-style work.** Phase window: avg CPU
1.6%, peak 7.6%, peak mem 8.5/31.5 GB; total compute was 83 s of a
mostly idle window. Verdict: recon numerics alone do not justify
c7i.4xlarge — retained because Phase 2 (many concurrent Lean
compile-verify loops) is the declared load; if Phase 2 telemetry
also shows this profile, downscale.

**Evidence pointers.** Full-precision k=8 CSV (304 MB, exact 12,844-
digit rationals) retained on mobius-fleet-1 at
`work/recon/samples_k8.csv`, sha256
`0d4791f1038023622db2bcc0cb4cad53bb489fb8c294a6b8a287710355cc34f2`;
trimmed float version committed here.

**Verdict.** deployment_status: n/a (diagnostic); candidate_status:
retained (harness + arrays on the box are the fleet's shared compute
substrate). Promotes nothing; closes nothing; frontier unchanged.
Known-unknown #3 (old Ramanujan-framing "synthesis constants")
superseded by this record's direct C/E landscape.

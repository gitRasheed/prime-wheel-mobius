---
id: 010
kind: experiment
status: closed
one_line: G2 first contact — prove the Quadratic-Sample Correlation Lemma (QSCL), the fully-classical exponential-sum input of the quadratic-dispersion angle
class: G2
parent: 008
---

## Declaration (frozen at launch commit; launch gated on blocking Codex review per law 13)

target: the proposer's named classical lemma (agent1.json,
  target_estimate (i)): for reduced fraction b/q and window length H,
  `|Σ_{r<H} e((b/q)(N+r+1)²)| ≪ H/√q + √q·log q`,
  with the companion multiplicity fact: for fixed q, the number of
  frequency collisions (pairs (d,d′) whose reduced difference has
  denominator q, at fixed root datum) is ≤ 2^ω(q) via root-counting
  of (n+1)² ≡ w (mod q). Primary deliverable: kernel-checked Lean
  statement + proof of the exponential-sum bound (constants explicit,
  any log-power loss permitted if declared in the statement);
  secondary (if mathlib support is thin): a complete rigorous paper
  proof with every step tagged standard/proved, submitted to Codex
  review, and the Lean statement stated with the proof stubbed as a
  clearly-marked open formalization task — that outcome counts as
  PARTIAL, not success.
approach: standard completion: split the quadratic Weyl sum by the
  Gauss-sum evaluation of complete sums mod q, bound incomplete sums
  by completion + geometric-series/kernel bounds; multiplicity via
  Hensel/CRT root counting. All classical; the experiment is whether
  it kernel-formalizes at bounded cost (mathlib has Gauss sums for
  characters; quadratic Weyl completion may need building).
analogue: textbook (e.g. Iwaniec–Kowalski ch. 8 completion method;
  cited, not rediscovered).
obstacles_addressed: none of the analytic obstacles bind — this is a
  q-aspect tool lemma, not a bound on the target object; the
  modulus-magnitude obstacle (006) is respected because QSCL is
  packet-conductor-level (small q), never full-modulus. The W2-G2a
  fixed-sign parity-locus rider and the W2-G2b inconclusive H=1
  arbiter are inherited open risks of the PARENT angle, not of this
  lemma — recorded so the lemma's success is not read as angle
  progress.
decision_rule: kernel-green ⇒ cluster Q gains its first proved tool;
  frontier delta: none (tool lemma; the angle's QSRD estimate remains
  fully open). PARTIAL per above. Failure/timeout ⇒ implementation
  closure of "QSCL formalizes cheaply", informal lemma stays
  UNVERIFIED; angle G2 unaffected mathematically.
promotion_gate: per law 5 on the exact Lean statement at a named
  mobius-synthesis agent-os commit (build --wfail + audit + #print
  axioms baseline).
viability_gate: sorry count decreasing; the Gauss-sum core either
  found in mathlib or half-built within the first agent run.
budget: two agent runs max.
review: BLOCKING Codex adversarial review of this declaration before
  launch (first attempt under angle G2). Review record: results/011.

## Gate revisions
(none)
## Run

Never launched. Blocking review (record 011) refuted the frozen
declaration before launch.

## Verdict

deployment_status: closed-unlaunched. closure_type: implementation —
scope: THIS declaration (statement as frozen). The companion
multiplicity claim is false as stated (x²≡1 mod 8 has 4 roots vs the
claimed 2^ω(8)=2; verified on receipt); the exponential-sum half
needs an even-q branch and is vacuous at H=1; the budget was
falsified by mathlib inventory (no Gauss-sum development in v4.24.0).
Angle G2 is mathematically UNAFFECTED — nothing here bounds or
refutes its mechanism; the corrected first-contact is record 012
(pair-to-root collision map + corrected fiber bound), per review 011.
candidate_status: retained (the review's corrected statement forms
are the seed of 012). Frontier delta: none.

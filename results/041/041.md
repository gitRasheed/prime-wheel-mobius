---
id: 041
kind: kernel
status: done
one_line: Degree-one energy criterion — name the three-slot contraction target and prove it equivalent to the recovered-wheel energy criterion (route work item 2)
parent: 039
class: kernel
---

Declared before results. Route work item 2 from the upstream handoff, and the
explicit "first concrete theorem I would assign" in the maintainer's route doc;
the DeepSeek PR-364 memo independently converges on the same next step ("state
the contraction theorem as an explicit named target, with the exact operator
and norm").

## Context established before declaration

- Upstream PR #362 ("Add exact three-slot Mertens degree-one projection") was
  merged into the stale `agent/three-slot-walsh-transition` branch, NOT main —
  the retargeting cleanup its own handoff flagged never happened. The file
  `RHLean/Analysis/ThreeSlotMertensDegreeOneProjection.lean` is therefore
  absent from the built library on main; a byte-identical copy sits in the
  `export_mobius_synthesis/` snapshot directory on main (not built).
- No `DegreeOneEnergy` statement exists anywhere in `RHLean/` on main
  (verified at a6cdfdf, post-#366).
- All imports of the stranded projection file (PrimeWheelThreeSlotRecovery,
  PrimeWheelRecoveredMertensCriterion) and of the companion synthesis
  checkpoint (PrimeSievePNTCentering, SquareRootTransportRealization, the
  Proof-namespace lemmas) exist on current main.

## Target (one PR, branch pr/degree-one-criterion)

1. Promote `ThreeSlotMertensDegreeOneProjection.lean` (verbatim from the
   stranded #362 merge = snapshot copy) into `RHLean/Analysis/` proper.
2. Optionally promote `ThreeSlotDegreeOneSynthesis.lean` likewise (deps all
   present on main).
3. New module `RHLean/Analysis/ThreeSlotDegreeOneCriterion.lean`:
   - `ThreeSlotDegreeOneEnergyBoundedStatement : Prop` — the route doc's
     exact skeleton: ∀ ε>0, ∃ C≥0, ∀ K,
     ‖((threeSlotWa K + threeSlotWb K + threeSlotWc K : ℤ) : ℂ)‖^2
       ≤ C · (K+1)^(1+ε).
   - Endpoint transfer in analytic form:
     ‖mertensSummatory X − mertensSummatory (4·(X/4))‖ ≤ 3, from record
     038's integer lemma `abs_moebiusPrefix_sub_fourCell_le` plus casts.
   - Forward: the statement implies
     `SqrtWheelRecoveredEnergyBoundedStatement` (constants: C ↦ 2C+18 via
     (a+3)² ≤ 2a²+18; monotone rpow in the base; (X+1)^(1+ε) ≥ 1).
   - Converse: the recovered-wheel criterion implies the three-slot
     statement (evaluate at X = 4K; constant C·4^(1+ε) via
     4K+1 ≤ 4(K+1) and mul_rpow). Hence an iff, stronger than the assigned
     implication.
   - `riemannHypothesis_of_threeSlotDegreeOneEnergy` chaining through
     `riemannHypothesis_of_sqrtWheelRecoveredEnergy`.

Design gets a Codex desk-only adversarial pass before Lean is written.
All builds on EC2 only.

## Gate

lake build RHLean --wfail green on top of origin/main (a6cdfdf); #print axioms
= [propext, Classical.choice, Quot.sound] for every new headline theorem; PR
from fork branch pr/degree-one-criterion.

## Run

2026-08-15. Codex desk-only design pass (task-msutvcbc-xvqny4): all six items
KEEP, none dropped. Fixes adopted: cast plumbing not to hard-code a
version-sensitive norm lemma name; confirmed same-ε constant 2C+18 correct
((a+3)^2 ≤ 2a^2+18 via (a-3)^2 ≥ 0), converse constant C·4^(1+ε) correct,
edge cases X∈{0..3}, K=0, small ε all benign.

Implementation (EC2 only). First build: 3 errors — (1) simp distributed
Int.cast_sub before normalizing the complex norm of the int cast → routed D2
through the repo's own Complex.sq_norm + normSq idiom (squared form, then
recover the norm bound by nlinarith with sq_nonneg (a-3)); (2) nlinarith for
(a+3)^2 ≤ 2a^2+18 needed the explicit product hint (a+3-m)(a+3+m) via hplus;
(3) rw [Real.one_rpow] fails against explicit Real.rpow spelling (pattern is
^-notation) → term-mode Real.rpow_le_rpow zero_le_one + simpa. Second build
of the module green; full gate green.

## Close

2026-08-15. Gate passed: lake build RHLean --wfail green, 7760 jobs, on main
a6cdfdf + promoted #362 modules; all six criterion theorems and the three
promoted headline theorems axiom-clean [propext, Classical.choice,
Quot.sound]. Route work item 2 discharged: the contraction target is now a
named Prop formally equivalent to SqrtWheelRecoveredEnergyBounded (both
directions, constants 2C+18 and C·4^(1+ε), no ε reparameterization), with RH
chain closure riemannHypothesis_of_threeSlotDegreeOneEnergy. Also repairs the
#362 mis-target: its two modules now reach the built library on main. Pushed
as 6b8b531 on pr/degree-one-criterion; upstream PR #367 opened.

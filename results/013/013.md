---
id: 013
kind: diagnostic
status: declared
one_line: G2 final gate — H=1 exponent-flow audit (review 011 alternative 2): does any surviving dispersion architecture contract the Mertens exponent?
class: G2
parent: 012
---

Per review 011 and the 012 verdict: with the bounded-fiber transfer
closed outright, angle G2 lives only if exact exponent bookkeeping
shows a contraction. Paper-level audit (informal channel):

1. Start from the inductive input Σ_{d≤D} M(d)² ≪ D^{1+2σ} (σ ∈
   (0, 1/2]); propagate every D-, N-, q-power through completion and
   any still-available large-sieve/dispersion step — now WITHOUT the
   dead bounded-fiber mechanism and WITH the true conductor scale
   q ≤ 4D² (012) and the modulus-magnitude obstacle.
2. Produce the explicit self-map σ ↦ F(σ) for the pointwise (H=1)
   route, with every loss term named. Kill condition (the parent's
   own): F(σ) ≥ σ anywhere on the range, or the D^{3/2}-type loss
   surviving at H=1 ⇒ the pointwise route is dead.
3. Also report the windowed (H ≥ N^{3/4}) exponent flow — if the
   window route contracts while H=1 does not, say so exactly (a
   window-only lemma is not a closure, per the standing warning).

Producer: Codex reasoning task. Output → this directory. Verdict
semantics: CONTRACTS (angle G2 earns a repaired-architecture
experiment) / NEUTRAL-OR-EXPANDS (G2 closes, scope = this
architecture family, blocking review before the class closure per
law 13) / BLOCKED (names the missing bookkeeping).

## Run
(appended)
## Results
(appended)

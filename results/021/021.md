---
id: 021
kind: diagnostic
status: done
one_line: H3 gate formulation — freeze the J₋₁-type discrete moment statement, resolve the sup-β inference chain, literature verdict
class: H
parent: 018
---

Record 018 (K2) left cluster Z alive with a named but unfrozen gate:
"the unconditional discrete moment Σ_{0<γ≤T} |F_K(ρ)|/|ρ| ≪ polylog —
H3's Ng-line J₋₁ object," where F_K(s) = Σ_{d≤K} μ(d) d^{−s}. Before
declaring an experiment against it, the gate must survive formulation.

## Questions (all to Codex, pure reasoning + literature)

- **Q1 (crux — inference chain):** write the complete chain from a
  discrete moment bound at zeta zeros to the Abel target
  |Σ_{d≤K} μ(d)R(⌊x/d⌋)| ≪ x^{1/2+ε}. Unconditionally, x^ρ = x^β with
  β possibly > 1/2, and an unconditional proof of the target IS RH —
  so identify exactly where sup β enters, whether zero-density
  estimates / contradiction structure (assume an off-line zero, derive
  a violated inequality) can carry it, and what the moment gate must
  therefore quantify over (zeros on the line only? all zeros with
  x^β weights? a density-weighted family?). If no coherent chain
  exists, say so — that kills the cluster at rung 2 regardless of the
  moment bound's truth.
- **Q2 (frozen statement):** state the exact gate as a single
  quantified inequality: ranges of T and K (relative to x; recall
  K = ⌊x/(y+1)⌋, y ≍ √x, so K ≍ √x), the weight (1/|ρ|, x^{β−1/2}/|ρ|,
  or other per Q1), and the required polylog power. This statement
  gets frozen verbatim into IDEAS.md as cluster Z's gate.
- **Q3 (literature):** what is known about discrete moments of Möbius
  Dirichlet polynomials at zeta zeros — Landau–Gonek explicit formula
  applied coefficient-wise (Σ_γ d^{−ρ} terms with d^{−1} < 1), Ng's
  J₋₁(T) results and their conditionality, Gonek's conjecture,
  large-sieve-at-zeros inequalities, Montgomery–Vaughan mean values
  combined with zero-density. Table: statement / conditional-on /
  strength vs the frozen gate.
- **Q4 (obstacle audit):** position of any candidate route against the
  seven declared obstacles of results/017/packet.md — in particular
  𝔉-closure (does squaring |F_K(ρ)| re-enter the closed class?) and
  circularity (obstacle 6).
- **Q5 (kill test + rung 0):** cheapest kill test for the frozen gate
  (numeric or desk), and the rung-0 deliverable (an unconditional
  bound weaker than polylog that would still be progress).

Verdict vocabulary: FORMULATES-CLEANLY / FORMULATES-WITH-REPAIR /
INCOHERENT (kills cluster Z at rung 2). Diagnostic promotes nothing;
any experiment follows in a separate declared record. Routing: Codex
(GPT-5.6) per policy; blocking — no experiment declaration until this
returns. Numerics, if any, are illustrative only.

## Run

Codex task task-msm6h37s-07mc9r (session 019fe7ef-4d6f-7630-b887-98a37c7b1ce8),
8m. Full report: `codex_report.md`.

## Results

**Verdict: FORMULATES-WITH-REPAIR.** The record-018 gate as named
(Σ_{0<γ≤T}|F_K(ρ)|/|ρ| ≪ polylog) is NOT a coherent sufficient
condition — two independent structural failures (both desk-exact, no
numerics needed):

1. **Off-line zeros suppressed:** the true normalized zero cost is
   x^{β−1/2}|·|/|ρ|; a line-only F_K moment silently assumes RH
   (obstacle 6). Zero-density estimates cannot repair it — one
   persistent zero at β = 1/2+δ contributes x^δ forever. Only a
   contradiction structure over ALL zeros with the x^{β−1/2} weight
   is admissible.
2. **Prime-power remainder omitted:** Möbius inversion J→π puts the
   m=2 term −(1/2)Σ_{d≤K}μ(d)Li(√(x/d)) ≈ −√x·F^log_{K,x}(1/2)/log x
   into the identity; the moment at nontrivial zeros carries no
   information about F_K(1/2), and bounding it already demands
   RH-scale Möbius cancellation at s = 1/2.

Also FALSE en route: replacing the log-weighted amplitude
Li_*((x/d)^ρ) by x^ρF_K(ρ)/(ρ log x) (the d-dependent log kernel must
be retained). K2's numerics probed only the conditional critical-line
main-zero term — consistent with 018's declared caveat, but narrower
than the cluster believed.

**Frozen repaired gate (Z-repaired), verbatim into IDEAS.md:** with
C₀(t) the exact prime-power+archimedean term of the repo-normalized
explicit formula and C_{x,K} = Σ_{d≤K}μ(d)C₀(x/d):

  ∃A,C,x₀ ∀x≥x₀ ∀K∈[κ₋√x,κ₊√x] ∀T∈[x²,2x²]:
  x^{−1/2}|C_{x,K}| + 2x^{−1/2}·Σ_{0<Im ρ≤T, all zeros, w/ mult.}
    |Σ_{d≤K} μ(d)·Li_*((x/d)^ρ)|  ≤  C(log x)^A.

∃A only — freezing A=3 from the K2 fit would promote numerics.
Literature: no verbatim theorem proves it, none falsifies it; Ng's
J₋₁ results are RH-conditional (the "unconditional Ng-line" phrasing
in 018 was wrong); Landau–Gonek gives signed first moments only —
secondary desk check: signed scale ≍ log K·log T, so polylog power
A < 2 is untenable. Rung 0 = unconditional 𝓜(x,K,T) = o(√x) (gives
S = o(x), PNT-strength); 𝓜 ≪ x^{1/4−δ} would be rung 1.

Frontier delta: **gate-correction** — cluster Z survives only as
Z-repaired; explicitly RH-strength, open, broader than a J₋₁ moment.
Error count: informal-math error #6 caught by review channel (018's
gate naming). Obstacle audit and kill details in `codex_report.md`.

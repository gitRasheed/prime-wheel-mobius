---
id: 028
kind: diagnostic
status: done
one_line: external replication — an independent agent stack ran our kill test 4 (mu x error sign correlation) on the Abel form; no sign-reinforcement pathology
---

External evidence received 2026-08-09 (upstream maintainer's
independent agent stack; raw source held in internal ops notes, not
this repo). Summary of the report:

- Their stack read this branch's IDEAS.md, adopted its own kill-test
  ranking ("tests 4, 7, 1 first"), and executed test 4 — the
  μ×error sign-correlation measurement — against the Abel form,
  using the landed PrimeSieveAbelIdentity module as the object
  definition.
- Prior AGAINST survival: an earlier upstream route (dyadic Li) was
  permanently closed on exactly this measurement — true Möbius signs
  reinforced the error mode relative to random signs.
- Result: the Abel form does NOT reproduce that failure.

## Interpretation (two separate facts)

1. **Process fact:** a second, independently built agent stack
   consumed the published branch, selected a kill test from our own
   ranking, executed it against a kernel object we shipped, and
   reported back. Cross-stack replication loop is live.
2. **Evidence fact:** the Abel coordinates avoid a sign-reinforcement
   disease that killed a sibling formulation — independent of our own
   measurements (016: flat cancellation coefficient 0.35–0.43), with
   a motivated prior against survival. Validates the coordinate
   choice.

## Caveats (standard, non-negotiable)

Numerics-grade: survives-a-kill-test raises credibility, proves
nothing, promotes nothing (obstacle 7 applies to their range exactly
as to ours). No frontier delta. Upstream's standing request: PRs
welcome for any route-closing or route-supporting kernel content,
provided they build against the repo's existing Lean cache (same
toolchain + mathlib pin, incremental, no new deps). Acted on: PR #4
(backward affine excursion, record 024) opened at head 7ed0075, full
gate re-verified, cache compliance stated in the PR body.

---
id: 028
kind: diagnostic
status: done
one_line: external replication — Fred's independent agent stack ran our kill test 4 (mu x error sign correlation) on the Abel form; no sign-reinforcement pathology
---

External evidence, received via Discord (Fred Viole, 2026-08-09
22:46 UK), verbatim:

> Agent feedback...
>
> I think just add PRs as anything that can even close off routes is
> useful at this point
>
> Which route, and why
>
> Their IDEAS.md ranks the kill tests itself — "run tests 4, 7, and 1
> first (highest information-per-hour)" — with test 4 = μ×error sign
> correlation, and makes the Abel form "now the leading object."
> gitRasheed just landed PrimeSieveAbelIdentity. So: test 4 on
>
> There was a concrete reason to expect this to die fast. Your dyadic
> Li route was closed on exactly this measurement — the registry's
> permanent finding is that "the true Möbius signs frequently
> reinforce that mode relative to random signs."
>
> Result: it does not reproduce that failure

## Interpretation (two separate facts)

1. **Process fact:** a second, independent agent stack (Fred's,
   different code, different models) consumed our published branch,
   selected a kill test from our own IDEAS.md ranking, executed it
   against the kernel object we landed (PrimeSieveAbelIdentity), and
   reported the outcome back. Cross-stack replication loop is live.
2. **Evidence fact:** the Abel form does NOT exhibit the
   sign-reinforcement pathology that killed Fred's dyadic-Li route
   (whose registry finding was that true Möbius signs reinforce the
   error mode relative to random signs). Independent of our own
   measurements (016: flat cancellation coefficient 0.35–0.43; C/E
   correlation 0.83→0.94), with a motivated prior AGAINST survival.
   This validates the coordinate choice: the Abel face avoids a
   disease that a sibling route died of.

## Caveats (standard, non-negotiable)

Numerics-grade: survives-a-kill-test raises credibility, proves
nothing, promotes nothing (obstacle 7, finite-range deception,
applies to Fred's measurement range exactly as to ours). No frontier
delta. Fred's standing request recorded: PRs welcome for ANY
route-closing or route-supporting kernel content, not only positive
theorems.

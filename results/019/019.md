---
id: 019
kind: experiment
status: declared
one_line: H2 repaired — full-constant KV bound on the Abel sum (rung 0), complete paper-grade proof per the K1-corrected route
class: H2
parent: 018
---

## Declaration (frozen at launch commit)
target: Theorem A (repaired): for every ε>0, x ≥ x₀(ε), with y ≍ √x,
  K = ⌊x/(y+1)⌋: |Σ_{d≤K} μ(d)·R(⌊x/d⌋)| ≤ x·exp(−(1−ε)·c₀·
  (log x)^{3/5}(log log x)^{−1/5}), c₀ the Korobov–Vinogradov
  constant — via the K1-corrected route (F_K completes to 1/ζ;
  doubled-height global-width VK contour; exact s=1 residue
  cancellation; π↔ψ bridge). Deliverable: complete written proof,
  every step tagged proved-inline / standard-with-citation; NO
  appeals to "clearly" at truncation terms.
approach: per results/018/k1_k7_report.md skeleton. Producer: Codex.
analogue: classical KV PNT machinery (cited); the sharpening is the
  full constant at argument x via the product-form contour.
obstacles_addressed: 𝔉-exit by inventory (kill-tested); parity ceiling
  conceded (this IS rung 0, not a route to rung 2); circularity clean
  (unconditional VK only); pinned void (Perron at the point).
decision_rule: proof survives blocking adversarial review (separate
  record, Opus) ⇒ status = reviewed-unconditional rung-0 result —
  explicitly NOT kernel-promoted (mathlib lacks VK machinery;
  formalization infeasible near-term, recorded as such per law 5 —
  reviewed ≠ kernel-checked, never conflated). Review finds a hole ⇒
  iterate once; unrepaired ⇒ implementation closure of the repaired
  route.
promotion_gate: kernel promotion NOT claimable; the achievable
  ceiling is "informal proof, all load-bearing steps reviewed" +
  numeric envelope check on the box.
viability_gate: complete draft in one producer run.
budget: one Codex run + one revision; one Opus review.
review: BLOCKING (first analytic proof attempt of the program).
## Gate revisions
(none)
## Run
(appended)
## Verdict
(appended)

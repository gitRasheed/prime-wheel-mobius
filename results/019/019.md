---
id: 019
kind: experiment
status: closed
one_line: H2 repaired — full-constant KV bound on the Abel sum (rung 0), complete paper-grade proof per the K1-corrected route
class: H2
parent: 018
---

## Declaration (frozen at launch commit)
target: Theorem A (repaired): for every ε>0, x ≥ x₀(ε), with y ≍ √x,
  K = ⌊x/(y+1)⌋: |Σ_{d≤K} μ(d)·R(⌊x/d⌋)| ≤ x·exp(−(1−ε)·c₀·
  (log x)^{3/5}(log log x)^{−1/5}), c₀ the Korobov–Vinogradov
  constant — via the K1-corrected route (F_K completes to 1/ζ;
  doubled-height global-width VK contour; exact s=1 residue
  cancellation; π↔ψ bridge). Deliverable: complete written proof,
  every step tagged proved-inline / standard-with-citation; NO
  appeals to "clearly" at truncation terms.
approach: per results/018/k1_k7_report.md skeleton. Producer: Codex.
analogue: classical KV PNT machinery (cited); the sharpening is the
  full constant at argument x via the product-form contour.
obstacles_addressed: 𝔉-exit by inventory (kill-tested); parity ceiling
  conceded (this IS rung 0, not a route to rung 2); circularity clean
  (unconditional VK only); pinned void (Perron at the point).
decision_rule: proof survives blocking adversarial review (separate
  record, Opus) ⇒ status = reviewed-unconditional rung-0 result —
  explicitly NOT kernel-promoted (mathlib lacks VK machinery;
  formalization infeasible near-term, recorded as such per law 5 —
  reviewed ≠ kernel-checked, never conflated). Review finds a hole ⇒
  iterate once; unrepaired ⇒ implementation closure of the repaired
  route.
promotion_gate: kernel promotion NOT claimable; the achievable
  ceiling is "informal proof, all load-bearing steps reviewed" +
  numeric envelope check on the box.
viability_gate: complete draft in one producer run.
budget: one Codex run + one revision; one Opus review.
review: BLOCKING (first analytic proof attempt of the program).
## Gate revisions
(none)
## Run

- Producer (Codex task msm3yxzf): complete proof delivered,
  `theoremA_proof.md` (18KB) — all truncations, the doubled-height
  bookkeeping, s=1 cancellation, and π-bridge proved explicitly;
  self-declared weakest step = the c₀ normalization convention
  (definitional: the proof gives every interior constant (1−ε)c₀).
- Blocking adversarial review: dispatched (Opus), verdict pending.
## Verdict

Review verdict (full text `review_full.md`): **PROOF-REPAIRABLE(1,2,3)
— but the repaired theorem is strictly WEAKER than the trivial route.**
The reviewer verified the entire contour machinery (completion lemma,
doubled-height geometry — K1's flagged risk genuinely defused,
truncation ledgers, exponent arithmetic, statement match). Decisive
finding: the proof's c₀ is the Ingham-level constant
sup_a a^{3/5}(5/3)^{1/5} ≈ 0.51× the actual KV PNT constant (Pintz's
1.96× factor needs zero-density estimates, structurally unavailable
to a pointwise-in-t contour); with best-known inputs on both sides,
triangle + best PNT beats this route by 1.293× in the exponent
constant. Supporting gaps: A = A(a) not absolute (quantifier order
sound, one-sentence fix); citation package needs the
Borel–Carathéodory lemma stated.

deployment_status: **closed — implementation scope**, unlaunched
revision declined by coordinator decision: the one permitted
iteration would buy a true-but-strictly-dominated theorem; spending
it violates the budget's purpose. Closure: "the product-form
contour route to a full-constant KV bound is Ingham-capped; reopens
only with a mechanism giving the contour access to zero-density
savings." candidate_status: retained — the proof document +
review constitute a correct, reviewed derivation of the Ingham-
constant bound and a precise impossibility ledger; the reviewer's
constant recomputation (2.1710·a^{3/5}, matching published 0.2123)
is a standing calibration reference. Rung-0 status: the trivial-scale
bound on the Abel sum is CLASSICAL (triangle + KV-PNT) — the honest
ladder note is that rung 0 in the beat-the-trivial sense remains
open, and H2's claim to it is dead. Fifth informal-mathematics
correction of the session, this one caught by cross-model review at
the constant level.

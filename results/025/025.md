---
id: 025
kind: diagnostic
status: done
one_line: H4 audit debt — renewal-kernel mass of agent6's exact-telescoping reduction (converges or diverges)
parent: 017
class: H
---

Agent6 (results/017/proposals/agent6.json) reduces the Abel face via
Heath-Brown/Harman decomposition + an exact telescoping lemma to
(i) type-II bilinear Gram blocks and (ii) a self-similar renewal edge
T(x) ≤ Σ_{a≤D} |g(a)|·(1 + c_a·T(x/a)) + blocks. The induction
converges only if the renewal kernel has mass < 1 — flagged by the
proposer as the unverified audit point and rung-2-fatal on failure
(edge trivial size x^{1+o(1)}). H4's node stays closed until audited.

## Questions (Codex, desk)

- **Q1:** for Heath-Brown's identity at level D = x^δ (k-fold, δ =
  1/10 as proposed), write the actual coefficient function g arising
  in the type-I extraction and compute the honest size of the renewal
  mass Σ_{2≤a≤D} |g(a)|·w_a with the scale weight w_a the reduction
  actually produces (derive it — do not accept the proposal's framing
  on faith). Is it < 1, ≍ (log D)^j for which j, or worse?
- **Q2:** if mass ≥ 1: does any repair exist inside the mechanism
  (fewer HB levels, Harman schedule with restricted supports,
  absorbing c_a decay from the x/a scale factor), or is divergence
  structural? If a repair exists, state its cost to the block
  structure.
- **Q3:** independent of Q1: is the exact telescoping lemma
  Σ_{n≤X}(g*1)(n)M(X/n) = Σ_a g(a) correct as stated (μ*1 = δ along
  the hyperbola, with the floor/truncation bookkeeping the proposal
  waves at)? Desk-verify or refute; no numerics.
- **Q4:** verdict on the H4 node: OPEN-SANCTIONED (mass condition
  holds or is repairable — state the repaired kernel), or
  CLOSED-KILLED (divergence structural; record the reopen condition).

Diagnostic promotes nothing; desk only; EC2 stays stopped.

## Run

Codex task task-msm91h5c-0dnwge (session
019fe831-12a5-73b0-a358-60bf9fabb262). Full report: `codex_report.md`.

## Results

**Verdict: CLOSED-KILLED — H4 node closed on five independent desk
grounds** (each proved exactly, no numerics):

1. Heath-Brown type-I coefficients are NOT supported on [1,D]: for
   primes p > D, g(p) = (1−k)log p. The proposal's support claim is
   false at the definition level.
2. HB decomposes Λ, not 1_P; dividing by log n destroys the exact
   convolution telescope. No exact scalar renewal kernel exists for
   the prime indicator.
3. The honest kernel on [1,D] is g(a) = (k−1)μ(a)·log a; at the
   rung-2 scale weight a^{−(1/2+ε)} its absolute mass is
   ≍ D^{1/2−ε} — polynomially divergent (even the proposal's
   too-favorable a^{−1} weight gives ≍ log D, not < 1).
4. The restricted-range edge is a two-parameter truncated hyperbola
   sum H(N,B), NOT the Abel functional at scale X/a — the claimed
   self-similarity is false (cutoff not self-similar under X ↦ X/a).
5. Every internal repair (fewer levels, Harman schedule, sign
   preservation) either removes the telescope, restores the original
   prime object, or destroys the block structure.

Salvage (correct pieces worth keeping): the abstract complete-
hyperbola telescope Σ_{n≤X}(g*1)(n)M(⌊X/n⌋) = Σ_{a≤X} g(a) is TRUE
(clean proof in report §Q3 — candidate for a future kernel identity
module), and the exact edge formula H(N,B) = 1 − Σ_{d≤N/(B+1)}
μ(d)(⌊N/d⌋−B) is exact. Reopen condition (frozen): an exact
prime-measure decomposition whose edge operator has norm < 1 at
homogeneity 1/2+ε with controlled central blocks, or a uniform
SIGNED contraction for the (k−1)μ(a)log a kernel — i.e., new
Möbius-correlation input, the program's known core.

Frontier delta: **node-closure** (H4 dead as proposed; kill grounds
1, 2, 4 are structural, not quantitative). Weekend queue is now
fully drained: H3 parked at rung 0 (022), H1 upgraded and
kernel-formalized (023/024), H4 killed (this record).

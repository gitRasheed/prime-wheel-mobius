---
id: 018
kind: diagnostic
status: done
one_line: kill-test wave 3 — K1 (H2 hinge desk audit), K7 (literature kills), K2 (zero-sum reconstruction settling cluster Z)
class: H
parent: 017
---

Priority tests from the 017 mapping inventory:

- **K1 (adjudicates H2, pure reasoning → Codex):** re-derive agent8's
  inner-Perron lemma in COMPLETED form (F_K = 1/ζ + O(K^{-(1-α)δ}·
  polylog) on the VK contour, per the mapping's diagnosis), with full
  w-truncation and residue bookkeeping; determine whether Theorem A's
  full-constant KV conclusion survives, and state the corrected
  theorem + proof skeleton, every step tagged.
- **K7 (literature, → Codex same run):** search for existing bounds on
  the hyperbola-truncated Σ_{d≤√x} μ(d)(ψ(x/d) − x/d) and for
  F_K-on-VK-contour lemmas — novelty check for H2's deliverable.
- **K2 (settles cluster Z, → Opus on EC2):** compute A_K(ρ)/M_K(ρ) at
  the first ~2000 nontrivial zeta zeros (mpmath, 25+ digits), x ∈
  {10⁶, 10⁷, 10⁸, 10⁹} at pinned square samples; test (i) the
  truncated zero-sum reconstructs the recorded Abel sums (residual vs
  T), (ii) THE decisive question: does Σ_γ |A_K(ρ)| x^{1/2}/|ρ|
  overshoot |S| by a growing factor — i.e., is inter-zero cancellation
  forced? Growing overshoot = cluster Z capped at trivial scale above
  rung 0 (kill per the cluster's shared condition).

Numerics declaration: mpmath multiprecision for zeros/Γ-terms, exact
integer μ/M, float64 arrays for cross-checks; ranges declared in
outputs; promotes nothing; deception caveat. Telemetry tag `zeros`.

## Run

- K1+K7 (Codex task msm2xeq6, retrieved via CLI): **K1 =
  LEMMA-REPAIRED-THEOREM-SURVIVES** — the false inner-Perron decay
  lemma is replaced by the 1/ζ-completion with a doubled-height,
  global-width VK contour; Theorem A's conclusion survives at every
  interior constant (1−ε)c₀. **K7 = KNOWN for the unspecified-c
  version** (immediate from pointwise KV); the full-constant
  sharpening not found verbatim in the literature — novelty is the
  constant, program value is rung 0. Full report: `k1_k7_report.md`.
- K2 (Opus, EC2): running — zero-sum reconstruction + forced-
  cancellation measurement.
## Results

- **K1: LEMMA-REPAIRED-THEOREM-SURVIVES** (doubled-height global-width
  VK contour; full interior constant (1−ε)c₀). H2 viable in repaired
  form only.
- **K7: KNOWN (weak form) / novelty = the full constant** (not found
  verbatim). H2's deliverable is honest rung-0 with a constant-factor
  sharpening.
- **K2: ABSOLUTE-SUMMABLE at rung-2 scale — cluster Z SURVIVES.**
  5000 zeros at 30 digits, 4 pinned + 192 ensemble samples, all
  cross-checks green (S reproduces 016 to 5e-10; complex128 verified
  to 14.7 digits vs mpmath). Overshoot of the absolute zero-sum over
  |S| grows as log²–log³x (fixed-height exponent in x: +0.008,
  r²=0.13 — statistically zero), NOT as a power of x; √x·polylog is
  inside x^{1/2+ε}. Truncated zero-sum converges to S plus the
  independently-computed secondary constant (agent3 kill (1) does not
  trigger). Coordinate correction recorded: ψ-vs-li units cost a
  log x, unit conversion not overshoot. Honest limits: RH assumed in
  input (internal question only); in-range the zero-side and
  triangle-side bounds coincide numerically (finite-range) but their
  fitted growth laws separate (0.640 vs 0.703). **Refined gate for
  cluster Z: the unconditional discrete moment Σ_{0<γ≤T}|F_K(ρ)|/|ρ|
  ≪ polylog — H3's Ng-line J₋₁ object. The obstacle is that moment
  problem, not forced cancellation.**

**Wave-3 verdict:** H2 repaired-viable (rung 0), cluster Z alive with
a named gate, H1 untested (its kill tests K3 are cheap and can run
with its experiment). Next: experiments 019 (H2 repaired proof) and
020 (H1 moment-hierarchy formalization).

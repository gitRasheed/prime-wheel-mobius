---
id: 009
kind: experiment
status: done
one_line: kernel-formalize the collapse identity — the H-side object equals −Σ_d M(d)·Π_d plus an explicitly defined drift term
class: G
parent: 006
---

## Declaration (frozen at launch commit)

target: a kernel-checked Lean theorem in mobius-synthesis (local
  `agent-os`) stating the EXACT collapse identity found informally in
  record 006 (agent6 Kill 1): with y = primorialPNTPrimeSieveCutoff k
  and the repo's exact objects, an identity of the form
  `C(y,x) − 2·E(y,x) = −(Σ_d M(d)·Π_d(y,x)) + R(y,x)` where Π_d is
  the reciprocal-interval prime count and R is a DEFINED remainder
  (smooth/bulk recombination), such that the centered version
  rewrites H_{k,n} as the centered signed sum plus centered R. The
  identity ONLY — no bound on R is attempted (its √-scale behavior is
  numerics-observed, analytically open).
approach: compose the repo's existing kernel-proved pieces
  (post-sqrt-gap identity, reciprocal reindex of E, all-plus mass
  splitting) into the rearranged form; new module only, no existing
  files touched except the manifest import.
analogue: none needed — finite rearrangement of proved identities.
obstacles_addressed: none apply (identity, not estimate); the
  definition-drift hazard is addressed by defining R explicitly in
  the statement, never as "the rest".
decision_rule: kernel-green ⇒ the signed sum becomes a formal object
  future experiments can target (frontier delta: none —
  coordinate-identity label). Failure ⇒ records the exact algebraic
  mismatch with the informal derivation; closes nothing.
promotion_gate: full `lake build RHLean --wfail` green with module
  imported + clean audit + `#print axioms` = baseline, at a named
  commit on mobius-synthesis `agent-os`.
viability_gate: statement elaborated and sorry count decreasing
  within budget; one relaunch on a named blocker.
budget: one agent run + one retry.
review: waived (kernel-answerable).

## Gate revisions
(none)
## Run

Opus attack agent, 3 build iterations, no mathematical rework. New
module `RHLean/Analysis/PrimeSieveCollapseIdentity.lean` + one sorted
manifest import, mobius-synthesis `agent-os` commit `8b941b6`. Gate:
`lake build RHLean --wfail` green (7603 jobs), audit passed,
`#print axioms` baseline-only on all 15 public declarations (agent
logs `build_tail.log`/`audit.log`/`axioms.log`); coordinator
independent recheck on the three main theorems: baseline-only
(`coordinator_axiom_recheck.txt`).

## Verdict

deployment_status: **promoted**. BOTH exact forms kernel-proved under
`√x < y`:
1. Prime-count form: `C − 2E = −Σ_d M(d)·Π_d + Msm` where
   `Msm = primeSieveSmoothMobiusMass` (Möbius mass of y-smooth n ≤ x)
   — the Li bulk cancels IDENTICALLY, correcting the informal
   `R = Msm − B`.
2. Discrepancy form: `C − 2E = −Σ_d M(d)·Δ_d + (Msm − B)` — the form
   record 006's numerics actually measured.
Plus the centered corollary rewriting H_{k,n} in collapse form, and
the load-bearing lemma `M_y⁺(x) = Msm + Σ_d M(d)·Π_d`.

**Size correction to record 006's "new program fact #1" (the key
scientific content):** the θ = 0.501 drift belongs to the
discrepancy-form remainder `Msm − B` (max |·|/√x = 0.23). In the
prime-count form, the signed sum and `Msm` are each ~x^0.9 objects
(slopes 0.981 / 0.908 on the same samples) that nearly cancel — so
"the problem concentrates in Σ_d M(d)Π_d" is misleading as stated.
The honest formal picture: H = −centered(Σ_d M(d)Δ_d) +
centered(Msm − B), with the remainder empirically at exactly renewal
scale and the signed discrepancy sum carrying the open content.
Cross-checked against 006's own artifact to 8.8e-9 / exact.

frontier_delta: none — `coordinate-identity` label. candidate_status:
retained (both forms are now formal objects experiments can name).
Second original kernel-checked contribution; report item for upstream.

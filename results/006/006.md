---
id: 006
kind: diagnostic
status: done
one_line: kill-test wave 1 — shared 10⁹ sieve framework; tests 4 (cluster L), 1 (cluster S), and the exact-identity subset of test 7 (cluster I)
class: G
parent: 005
---

First post-seeding wave, per the record-005 disposition rule:
kill-test diagnostics before proof attempts. Implements the top of the
mapping report's deduplicated inventory (highest information/hour).

## Method (declared before running)

Shared framework on mobius-fleet-1 (phase tag `killtests`): exact μ
sieve + Mertens + prime counts + Li to N = 10⁹ (int8/int32/float64;
~17 GB, within 31 GB).

Tests, each taken verbatim from the proposer's own `kill_test` field
in `results/005/proposals/` (the implementing agent reads the JSONs
and implements the proposer's stated computation; any deviation is
declared in the results):

- **T4 (arbitrates cluster L, from agent5):** μ(d) × err(x/d)
  sign-correlation structure at pinned square samples, large blocks.
  Kill condition per proposer: strong positive resonance between
  Mertens/μ weights and reciprocal-interval discrepancies.
- **T1 (arbitrates cluster S, from agents 0 and 7):** per-frequency /
  per-packet survival of the C−2E cancellation — Ramanujan-packet
  amplitudes A_q at small k (dense moduli, k ≤ 3–4) vs the
  renewal-scale template; Fourier ℓ¹ mass of the combined observable.
  Kill condition: energy spreads so no packet keeps the correlation.
- **T7-subset (arbitrates cluster I, from agents 6, 7, 9):** exact
  identity checks — kernel-collapse drift + per-d cancellation ratio
  (agent6), coconductor packet-sum exactness (agent7), Abel
  resummation boundary term (agent9). Exact arithmetic where the
  proposer's statement is exact.

Deferred to wave 2 (need bespoke formulation work): agent2's Buchstab
Type-II equality, agent3's Perron contour reproduction, agent1's
square-moduli locus at 10⁹, agent8's window variance mass location.

## Numerics-channel declaration

Exact integers for μ, M, combinatorial masses and identity checks;
float64 for Li/discrepancy quantities with reported error; range
x ≤ 10⁹; deterministic. Kill semantics per PROOF_OS: a failed
numeric kill test closes the *cluster assumption* at implementation
strength only; passing kills nothing and promotes nothing.
Utilization telemetry per the standing directive.

## Run

Implementation agent (single run + two in-run fixes: a wrong hardcoded
checkpoint of its own, and the Q_8 big-int str crash). Framework:
exact sieve to 10^9 in 70 s, cross-validated byte-identical against
the record-004 sieve on the overlap, Mertens checkpoints + micro-
asserts green in every script. Compute ~12 min, CPU 74-80% during
heavy phases (telemetry tag `killtests`), peak mem 21.9 GB. Artifacts:
CSVs/logs + `killtest_report.json` here and on the box.

## Results (verdicts against each proposer's own stated kill condition)

- **T4 / G3 (agent5, telescoped BTD): KILLED** at implementation
  strength. (a) corr(mu(d), err(x/d)) systematically positive in all
  four blocks k=5..8 (aggregate t~38 at k=8) — his "most decisive"
  condition fires; honest caveat: per-sample effect is bounded
  (z~0.3, non-accumulating), recorded. (b) Structural kill: pair
  multiplicity r(h) concentrates at smooth/lcm-rich shifts
  (r(360360) ~ x^0.78; max r(h)/sqrt(x) grows 5.4 -> 229) — the
  Deshouillers-Iwaniec sub-square step is dead as proposed. His V(x)
  test survives. Reopens when: a variant that excises smooth-shift
  classes and survives (b).
- **T1 (agent0, coboundary): KILLED twice.** l1 Fourier mass M_k ~
  X^{0.52-0.55} (needs X^eps) — exact at k=2,3, integral-validated
  k=4; and its load-bearing numerology Q_k ~ p_k X is false by factors
  10^5.9 (k=3) to 10^12835 (k=8).
- **T1 / G1 (agent7, coconductor): exactness SURVIVES** (packet sum
  = H integer-exactly, k=2,3; Gauss majorant never violated, max
  ratio 0.72); **decisive renewal-vs-raw discrimination INCONCLUSIVE**
  (his two templates are numerically degenerate below X~10^7 — needs
  the 10^8-10^9 range, wave 2). Same modulus-magnitude correction
  applies to his "Q ~ 6U" wording.
- **T7 (agent6): split verdict — the wave's biggest finding.**
  Kill 1 SURVIVES exactly: the derived collapse
  `C - 2E = -Sum_d M(d)*Pi_d + R` verified INTEGER-EXACTLY at all 154
  sampled x, k=4..8, with drift R at renewal scale (theta = 0.501,
  |R| <= 0.228*sqrt(x)) — far inside his 0.6 kill line. Kill 2
  KILLED: per-d cancellation ratio 0.90-0.99 rising to ~0.99 at k=8 —
  the C-2E cancellation lives ACROSS d (statistical), not per-d
  (algebraic). His Kill 3 (L2 across-d) is the wave-2 arbiter.
- **T7 (agent9): SURVIVES** — telescoped identity reproduces E to
  1.6e-10; dyadic boundary terms <= 0.154*sqrt(x), no growth.

**New program facts** (numerics-channel status: verified in range,
promote nothing):
1. **Collapse identity**: H-side object = -Sum_d M(d)*Pi_d + O(sqrt x)
   with the drift empirically pinned at exponent 0.501. The open
   problem concentrates in the signed sum Sum_d M(d)*Pi_d.
   Informal derivation + exact numeric verification; kernel
   formalization = experiment 009.
2. **Modulus-magnitude correction**: Q_k exceeds 6*U_k by 10^5.9 to
   10^12835 — any argument scaling with sqrt(Q_k) or assuming
   Q_k ~ U_k numerology is dead on arrival. Declared obstacle added
   to AGENTS.md.
3. Smooth-shift pair concentration (r(h) at lcm-rich h ~ x^0.78) —
   standing hazard for any shift-pairing construction.

**Cluster scoreboard**: I (algebraic) — dead in per-d form, superseded
by the collapse identity; L (l2 repair) — wounded (T4a) but alive,
wave-2 L2 test is the arbiter; S (per-packet) — alive, inconclusive,
needs range; Q (quadratic dispersion) — agent0's version dead,
conductor-level versions (G1, G2) alive with the modulus correction;
R (contraction) — untested this wave.

**Correction appended 2026-08-09 (record 009):** the collapse identity
is kernel-proved in BOTH exact forms, but fact #1's size attribution
needed correction — θ = 0.501 belongs to the discrepancy-form
remainder `Msm − B`; the prime-count-form signed sum is a ~x^0.98
object nearly cancelled by `Msm` (~x^0.91). See results/009 verdict.

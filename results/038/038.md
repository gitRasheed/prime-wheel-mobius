---
id: 038
kind: port
status: open
one_line: Port PR #8 module set to RH_Lean (active sandbox) + three-slot mesh assessment + four-cell endpoint transfer (route work item 1)
parent: 037
class: kernel
---

Declared before results. Trigger: user direction 2026-08-15 — RH_Lean is the
active sandbox, mobius-synthesis is the public snapshot; "shift this work
over to the RH_Lean repo and see if the mobius-synthesis PR meshes with what
we have." Upstream pivoted to the three-slot Möbius degree-one route
(RH_Lean PR #361; four-cells (4k+1..4k+4), slot 4 dead, W_a+W_b+W_c = M(4K),
R−2H corrected field, collision-involution Route A, finite-difference-fiber
Route B, square-transport Route C).

## Findings driving the port

- RH_Lean core (397 modules) contains every dependency of our five modules
  (telescope, abstract affine, sharp affine, two-obligation, synthesis
  witness) — same toolchain and mathlib pin as mobius-synthesis.
- RH_Lean core does NOT contain the five modules; the freshly prepared
  snapshot export (RH_Lean PR #363) also lacks them, so the next public
  refresh would drop PR #8's content. Durable home = RH_Lean.
- RH_Lean core still carries the OLD overstated excursion docstrings (the
  snapshot got our corrections via PR #8's branch only).
- Route B (finite-difference fibers D_S f(x) = Σ_{d|∏S} μ(d) f(⌊x/d⌋)) is
  literally our substrate: S(y,x) is D_{primes≤y} applied to the prime
  discrepancy; the telescope is the exact inversion identity on that
  interface; the affine slope is a modulus of continuity for it.
- Route doc "Recommended order of work" item 1 (bounded endpoint transfer
  M(4K) → M(X)) is unclaimed: |moebiusPrefix X − moebiusPrefix (4⌊X/4⌋)| ≤ 3
  plus the exact cell resummation Σ_{k<K} fourSlotCellSum k =
  moebiusPrefix (4K).

## Plan

1. Fork OVVO-Financial/RH_Lean → gitRasheed/RH_Lean (none exists).
2. Branch pr/renewal-affine-port off origin/main in a scratch worktree
   (user's RH_Lean checkout untouched — it carries their own .github edits).
3. Port the five modules + the two docstring repairs; add new module
   MobiusFourCellEndpointTransfer (work item 1); wire RHLean.lean manifest.
4. Build + axiom audit on EC2 (fresh clone, shared mathlib cache).
5. Deliver mesh report; open RH_Lean PR after green; mobius-synthesis PR #8
   reply updated to note the port.

## Gate

lake build RHLean --wfail green on the port branch; #print axioms clean for
all ported/new theorems. Numerics none (exact identities only).

## Run

(open)

---
id: 003
kind: diagnostic
status: done
one_line: Phase 0 — kernel builds + axiom audits in all three repos; extraction of synthesis open targets; verify the no-external-axiom RH-bridge claim
class: D
parent: 001
---

Evaluator-health diagnostic (PROOF_OS law 5) across the three-repo
program, launched on Rasheed's direction (journal 2026-08-09-3). No
statement promotes on grep or CI badges; this record produces the
local kernel evidence everything later stands on.

## Method (declared before running)

Per repo, at the named commit on its local `agent-os` branch:

| Repo | Commit | Build dir | Command |
|---|---|---|---|
| prime-wheel-mobius | branch `agent-os` (see run log) | `formalization/` | `lake build RHLean --wfail` |
| square-block-mobius | `8ff51e1` (new export) | `lean/` | `lake build RHLean --wfail` |
| mobius-synthesis | `e87f728` | repo root | `lake build RHLean --wfail` |

plus each repo's own `scripts/audit_assumptions.sh`. Toolchain:
elan 4.2.3, `leanprover/lean4:v4.24.0`, mathlib `v4.24.0` (all three
pins agree). Mathlib oleans fetched via `lake exe cache get` where
available — cached mathlib artifacts are standard practice and do not
weaken the audit of *project* modules, which compile locally; noted
honestly as the arithmetic model of this diagnostic.

Additionally, for mobius-synthesis after a green build:

1. Extract the exact Lean declarations of the open targets
   (`NonzeroResponseRHScale`, the C−2E route object) and the terminal
   RH-bridge theorem, verbatim, into this record and then `AGENTS.md`.
2. Run `#print axioms` on the terminal bridge theorem — the repo's
   claim (CURRENT_PROOF_ROUTE.md §7) that no external Mertens→RH
   premise remains is CONFLICTED-pending-audit until this shows only
   the standard foundational axioms.

Outcomes bind nothing beyond evaluator-health facts: a red build or
dirty audit is a review trigger (law 5), never quietly patched.

## Run

(build logs land in this directory as `build_<repo>.log`,
`audit_<repo>.log`; launch descriptors below as runs start)

**2026-08-09, builds launched** (elan 4.2.3; three parallel
`lake exe cache get && lake build RHLean --wfail`, logs in this dir).

**2026-08-09, extraction sub-task complete** (read-only agent over
`mobius-synthesis @ e87f728`; findings verified against source):

- Open targets, verbatim locations:
  - `NonzeroResponseRHScale : Prop`
    (`RHLean/Analysis/MobiusSynthesisBoundary.lean:46`) — ∀ε>0,
    `NonzeroResponsePowerBound (1/2+ε)` on
    `squareWheelNonzeroSampleResponse` at square-prefix samples.
  - `ProjectedRenewalQuadraticBoundedStatement (Λ) : Prop`
    (`RHLean/Proof/CanonicalGapAncestryQuadraticClosure.lean:122`) —
    the quadratic/Gram form of the open estimate; the one the terminal
    bridge consumes.
- **Bridge-claim verdict (the CONFLICTED item from journal
  2026-08-09-2): resolved, nuanced.**
  - Forward direction is genuinely criterion-free:
    `projectedRenewalQuadraticBounded_imp_riemannHypothesis_unconditional`
    (`RHLean/Proof/TerminalMertensForward.lean:37`) takes only `0 ≤ Λ`
    and the open estimate. It rests on
    `riemannHypothesis_of_mertensEnergy`
    (`RHLean/Analysis/MertensEnergyRHForward.lean:66`), an in-repo
    proof of Mertens-energy → RH via Mellin continuation and
    completed-zeta reflection using only mathlib ingredients.
  - The *equivalence* (iff) still takes the typed premise
    `ClassicalMertensRHCriterion`
    (`SquarePrefixMertensBridge.lean:319`), never constructed; the
    reverse RH → Mertens is explicitly not asserted.
  - Consequence: for the program's purpose (prove the estimate ⇒ get
    RH), the chain is now premise-free. Prime-wheel's known-unknown #7
    (should the classical bridge be formalized?) is answered for the
    forward half by the synthesis repo itself.
- Source-level audit: zero `sorry`/`admit`, zero project `axiom`/
  `constant` declarations across all 249 `.lean` files (three textual
  "constant" hits are prose).
- `TerminalAxiomAudit.lean` build-time-guards `#print axioms` =
  `[propext, Classical.choice, Quot.sound]` on three terminal
  theorems — but **predates the criterion-free route**: it does not
  audit `..._unconditional` or `riemannHypothesis_of_mertensEnergy`.
  Post-build action added: run `#print axioms` on those two directly.
- Minor flag: 249 files on disk vs 248 imported by the manifest — one
  module unimported (unidentified; unimported files escape the build
  and the audit). To identify post-build.

## Results (2026-08-09)

**Evaluator note.** Verification ran on the new EC2 build server
(`mobius-fleet-1`, i-0fc2bb4713d1486ca, Ubuntu 24.04, elan → lean4
v4.24.0, mathlib v4.24.0 via `lake exe cache get`; see
`mobius-ops/README.md`). The local drvfs builds declared above were
superseded mid-run and stopped (partial logs retained,
`build_*.log`); repos on EC2 cloned from the public GitHub mains —
commit identity per repo recorded in `ec2_audits.log` and identical
in content to the local `agent-os` branch bases.

**Builds: all green** (`ec2_bootstrap.log`): prime-wheel 1m43s,
square-block 3m29s, synthesis 4m21s (`lake build RHLean --wfail`,
zero warnings-as-errors failures).

**Audits: all clean** (`ec2_audits.log`): each repo's own
`audit_assumptions.sh` passes — no `sorry`/`admit`, no project-local
`axiom`/`constant`. Square-block's former UNOBSERVABLE status is
resolved: it kernel-verifies. Known-unknown #6 closed.

**Kernel axiom audit of the unconditional bridge**
(`ec2_axiom_check.txt`): both
`RHLean.Proof.TerminalMertensForward.projectedRenewalQuadraticBounded_imp_riemannHypothesis_unconditional`
and `RHLean.Analysis.riemannHypothesis_of_mertensEnergy` depend on
exactly `[propext, Classical.choice, Quot.sound]` — the standard
foundational baseline, nothing else. **The forward chain (open
estimate → RH) is kernel-verified premise-free.** The 2026-08-09-2/-4
CONFLICTED item is fully resolved in the repo's favor.

**Definition-drift finding (the significant one).** The one on-disk
module missing from the 248-import manifest is
`RHLean.Analysis.MobiusSynthesisBoundary` — the file defining the
README's headline predicate `NonzeroResponseRHScale`. It compiles
clean against the built library, but **nothing imports or references
it**: no kernel-checked theorem connects `NonzeroResponseRHScale` to
`ProjectedRenewalQuadraticBoundedStatement`, the statement the
terminal bridge actually consumes. Consequences: (1) the program's
canonical attack target is `ProjectedRenewalQuadraticBoundedStatement`
(wired to RH); `NonzeroResponseRHScale` is documentation-grade until a
connecting theorem exists; (2) this is a gap worth reporting to upstream
(README presents the orphan as canonical); (3) candidate first
kernel-checked contribution for this program: prove the connecting
equivalence (or implication) between the two.

**Verdict.** deployment_status: n/a (diagnostic); candidate_status:
retained. Evaluator health GREEN across all three repos for the first
time under this program. Frontier per the PROOF_OS frontier-delta
rule: unchanged — nothing here is analytic progress; it is
`formalization-support` establishing the verified baseline. The
frontier is exactly `ProjectedRenewalQuadraticBoundedStatement`
(∀Λ≥0-form), from which RH follows premise-free.

---
id: 043
kind: panel
status: declared
one_line: Round-5 multi-agent panel on ThreeSlotDegreeOneEnergyBoundedStatement — blind generation, adversarial kill, numeric falsification, then one kernel lemma or a certified kill report
parent: 041
class: frontier
---

Declared before results. With PRs #367 and #368 merged, every bridge between
the three-slot degree-one mode and RH is kernel-verified; the only remaining
mathematics is the cancellation estimate itself. This record runs the first
structured attack.

## Setup

Phase 0 dossier frozen at declaration time (`dossier.md` in this directory):
exact target Prop, kernel-verified machinery inventory (through upstream
main bd95c72, post-#372), measured numerics (records 032/040 + PR #372
diagnostics), auto-kill constraints (Ω±(√x) guardrail, route-doc §6 bans,
section-J certified kills, the #372 trilemma, closed-route list, lane
discipline for open PR #373), and the dead-end registry.

Ideation scales via Claude subagents (Codex reserved for desk design and
post-implementation audit of any Lean-bound survivor, per its usage limits):

- Phase 1 — blind generation: 12 generators, two per mechanism class
  (renewal/self-similarity, involution + defect charging, fresh-prime energy
  induction, cube-spectral with cross terms, signed second moment) plus one
  trilemma-aware wildcard and one contrarian no-go prover; schema-forced
  structured proposals; each blind to the others.
- Phase 2 — kill panel: after inline dedup, ≥2 adversarial refuter lenses
  per surviving proposal (constraint-compliance vs the dossier's D-items;
  classical-analog/Ω± check); default to kill.
- Phase 3 — numeric falsification of survivors' computable predictions
  (Python, local; numerics falsify never prove).
- Phase 4 — at most one survivor goes to Lean: Codex desk pass, EC2 build,
  standard PR discipline. Only kernel output is promotable.

## Gate

Exactly one of: (a) a first kernel lemma shipped through the standard cycle
(lake build RHLean --wfail green on main; axioms [propext, Classical.choice,
Quot.sound]; PR from fork), or (b) a certified kill report naming each
proposed mechanism and the dossier constraint or measured falsifier that
killed it, appended to the dead-end registry. Both close the record as done;
anything else closes it as failed.
